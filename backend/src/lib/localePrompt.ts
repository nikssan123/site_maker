/**
 * App default locale: Bulgarian (bg). All AI agents and generated apps must follow this.
 * Keep JSON keys / route path segments ASCII; user-visible copy in Bulgarian.
 */

export const BG_LANGUAGE_BLOCK = `

═══ ЕЗИК: БЪЛГАРСКИ (задължително) ═══
- Всички съобщения към крайния потребител (чат, обяснения, етикети в този продукт) — на български език.
- В генерираното React приложение: всички видими текстове (Typography, Button, placeholder, Snackbar, Dialog, заглавия, таблици, празни състояния, грешки, примерни/seed данни) — на български.
- Вътрешният JSON блок \`\`\`plan\`\`\`: ключовете остават на английски (appType, pages, features, hasDatabase, dataModels, полетата на моделите като идентификатори). Стойностите за потребителя — description, елементите в pages/features като четими имена — на български. За URL пътища използвай латиница (напр. /book, /services) или транслитерация; етикетите в навигацията на български.
- В JSON вътре в \`\`\`plan\`\`\` ползвай САМО прави ASCII кавички " — не „ или “; иначе приложението не може да прочете плана.
- Коментари в кода и имена на променливи могат да са на английски за яснота.
- Валута: потребителският език може да е български, но всички цени, суми, такси, seed данни и checkout payload-и трябва да са в EUR. Показвай "€" или " €"; за payload-и използвай "eur". Забранени са "BGN", "bgn", "лв", "лева", "лев", "USD", "$" и "GBP".
`;

export const BG_CODEGEN_RETRY = `Изходът беше отхвърлен: трябва да е валиден JSON само с обект "files" от път → съдържание. Не извеждай server.js или компоненти като суров текст — само вътре в JSON низовете. Отговори само с JSON обекта.`;
